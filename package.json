{
  "name": "nicepage.com-grid-layout",
  "version": "1.0.0",
  "description": "Grid layout",
  "main": "index.js",
  "scripts": {
    "watch:sass": "sass sass/main.scss style.css -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.55.0"
  }
}
